apiVersion: v1
kind: Service
metadata:
    name: azurecoforgedotnetframeworkwebbapi
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: azurecoforgedotnetframeworkwebbapi